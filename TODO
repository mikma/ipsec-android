* Flush setkey SAs for changed peers.

* Support for "virtual" IP addresses.

* Detect IP address changes

* Include no connected peers in notification


Policy handling
---------------

Startup: Build all peer configs, Flush all SPDs and SADs

Disconnect: Delete SPD for peer, Disconnect peer, Delete SAD for peer, Disable Peer
